name: Deploy to Google Cloud Run

on:
  push:
    branches:
      - main

env:
  PROJECT_ID: conscious-club
  SERVICE_NAME: cc-server
  REGION: us-central1

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker us-central1-docker.pkg.dev

      - name: Authorize Docker push
        run: gcloud auth configure-docker

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        run: |
          docker buildx build --cache-from type=gha --cache-to type=gha,mode=max \
            -f apps/backend/Dockerfile \
            -t us-central1-docker.pkg.dev/${{ env.PROJECT_ID }}/cc-server/cc-server:${{ github.sha }} \
            --push \
            .

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image us-central1-docker.pkg.dev/${{ env.PROJECT_ID }}/cc-server/cc-server:${{ github.sha }} \
            --platform managed \
            --region ${{ env.REGION }} \
            --allow-unauthenticated \
            --set-env-vars "\
              NODE_ENV=production,\
              AUTH_SECRET=${{ secrets.AUTH_SECRET }},\
              DATABASE_URL=${{ secrets.DATABASE_URL }},\
              GCLOUD_PROJECT_ID=${{ secrets.GCLOUD_PROJECT_ID }},\
              GCLOUD_BUCKET=${{ secrets.GCLOUD_BUCKET }},\
              GCS_CLIENT_EMAIL=${{ secrets.GCS_CLIENT_EMAIL }},\
              GCS_PRIVATE_KEY=${{ secrets.GCS_PRIVATE_KEY }}\
            " \
            --timeout=900s
