import React from "react";

export const metadata = {
  title: "Add Blog Posts",
  description: "Add Blog Posts page",
};

const page = () => {
  return (
    <div>
      <h2>Admin Add Blog Posts</h2>
    </div>
  );
};

export default page;
